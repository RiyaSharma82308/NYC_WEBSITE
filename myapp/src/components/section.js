import React from "react";

const Section = ({ children }) => {
  return (
    <section
      style={{
        margin: "1.45vw 0 1.45vw 0"
      }}
    >
      {children}
    </section>
  );
};

export default Section;
